<%= simple_form_for [:admin, part], method: methode, authenticity_token: true, wrapper: :custom_form_horizontal,
    class: "form-horizontal form-groups-bordered" do |f| %>
  <div class="row">
    <div class="col-md-12">
      <div class="card-group joined" id="accordion-test">
        <div class="card">
          <%= link_to "#collapse-fr",:data => {:toggle => "collapse" , :parent =>"#accordion-test"}  do %>
            <div class="card-header">
              <h4 class="card-title">
                  <% if methode == :post %>
                    Ajouter
                  <% else %>
                    <%= part.part_desc %>
                    <span>
                    (ID <%= part.id %> - Dernière mise à jour: <%= l(part.updated_at, format: :long) %>)
                    </span>
                  <% end %>
              </h4>
            </div>
          <% end %>
          <div class="collapse show" id="collapse-fr">
            <div class="card-body">
              <%= f.input :part_desc, label: "Description" %>

              <hr>
              <div class="form-group row">
                <div class="col-sm-offset-3 col-sm-6">
                  <%= f.button :submit, "Enregistrer", class: 'btn btn-success' %> &nbsp;&nbsp;
                  <%= f.button :submit, 'Enregistrer et continuer', class: 'btn btn-success set-lang', lang: 'fr' %> &nbsp;&nbsp;
                  <%= link_to "Retour", admin_parts_path, {:class=>"btn btn-default"} %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
